# Development dependencies for audiobookify.
#
# pyproject.toml is the single source of truth -- this file just delegates to
# the [dev] extra so the two can't drift apart.
#
# Install with:  pip install -r requirements-dev.txt
#           or:  pip install -e ".[dev]"

-e .[dev]
